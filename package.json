{
  "name": "numbersense-practice",
  "version": "1.0.0",
  "private": true,
  "description": "A 10-day number sense practice app for Cloudflare Workers and Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-cloudflare.mjs",
    "cloudflare:build": "node scripts/build-cloudflare.mjs",
    "deploy": "npm run build && npx wrangler deploy",
    "cloudflare:preview": "npm run build && npx wrangler dev",
    "cloudflare:deploy": "npm run build && npx wrangler deploy",
    "cloudflare:pages:deploy": "npm run build && CLOUDFLARE_ACCOUNT_ID=5549f41d5fa87a72d0a1f313203437b1 npx wrangler pages deploy public --project-name numbersensepractice"
  }
}
